export interface Feed {
    author: string;
    description: string;
    image: string;
    link: string;
    title: string;
    url: string;
}
export interface FeedModel {
    title: String,
    slug: String
}